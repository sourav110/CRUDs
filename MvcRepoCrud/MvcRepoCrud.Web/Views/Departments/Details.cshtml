@model MvcRepoCrud.Entities.Department

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-info">Details</h2>
<hr />

<form method="post">
    <input name="Id" value="@Model.Id" hidden />

    <div class="row">
        <div class="col-md-10">
            <div class="form-group">
                <label>Name</label>
                <input name="Name" value="@Model.Name" class="form-control" readonly />
            </div>

            <div class="form-group">
                <label>Abbr</label>
                <input name="Abbr" value="@Model.Abbr" class="form-control" readonly />
            </div>
        </div>
    </div>
    <a href="@Url.Action("Index", "Departments")" class="btn btn-warning">Back to List</a>
</form>